import { UserProfilPipe } from './user-profil.pipe';

describe('UserProfilPipe', () => {
  it('create an instance', () => {
    const pipe = new UserProfilPipe();
    expect(pipe).toBeTruthy();
  });
});
