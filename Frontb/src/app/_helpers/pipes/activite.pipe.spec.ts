import { ActivitePipe } from './activite.pipe';

describe('ActivitePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivitePipe();
    expect(pipe).toBeTruthy();
  });
});
