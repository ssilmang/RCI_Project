import { ControlePipe } from './controle.pipe';

describe('ControlePipe', () => {
  it('create an instance', () => {
    const pipe = new ControlePipe();
    expect(pipe).toBeTruthy();
  });
});
