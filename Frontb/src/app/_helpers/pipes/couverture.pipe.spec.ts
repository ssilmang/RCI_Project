import { CouverturePipe } from './couverture.pipe';

describe('CouverturePipe', () => {
  it('create an instance', () => {
    const pipe = new CouverturePipe();
    expect(pipe).toBeTruthy();
  });
});
