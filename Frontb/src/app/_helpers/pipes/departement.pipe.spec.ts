import { DepartementPipe } from './departement.pipe';

describe('DepartementPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartementPipe();
    expect(pipe).toBeTruthy();
  });
});
