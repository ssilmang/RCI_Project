import { Departement2Pipe } from './departement2.pipe';

describe('Departement2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Departement2Pipe();
    expect(pipe).toBeTruthy();
  });
});
