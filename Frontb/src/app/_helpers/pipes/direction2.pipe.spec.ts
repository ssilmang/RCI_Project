import { Direction2Pipe } from './direction2.pipe';

describe('Direction2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Direction2Pipe();
    expect(pipe).toBeTruthy();
  });
});
