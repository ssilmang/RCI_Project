import { PeriodicitePipe } from './periodicite.pipe';

describe('PeriodicitePipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodicitePipe();
    expect(pipe).toBeTruthy();
  });
});
