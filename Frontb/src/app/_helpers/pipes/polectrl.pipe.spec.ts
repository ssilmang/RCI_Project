import { PolectrlPipe } from './polectrl.pipe';

describe('PolectrlPipe', () => {
  it('create an instance', () => {
    const pipe = new PolectrlPipe();
    expect(pipe).toBeTruthy();
  });
});
