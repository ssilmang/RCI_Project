import { SearchCtrlPipe } from './search-ctrl.pipe';

describe('SearchCtrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCtrlPipe();
    expect(pipe).toBeTruthy();
  });
});
