import { Service2Pipe } from './service2.pipe';

describe('Service2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Service2Pipe();
    expect(pipe).toBeTruthy();
  });
});
